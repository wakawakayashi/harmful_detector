# v3: 3 classes (gun, knife, bat). curated+gun2 in place, Zenodo remapped+merged.
# Relative to the repo root. datasets/ is gitignored — supply your own data here.
path: datasets
train:
  - haris-weapon-detection-dataset-curated/train/images
  - gun2_yolo/images
  - zenodo_yolo/train/images
val:
  - haris-weapon-detection-dataset-curated/val/images
  - zenodo_yolo/val/images
nc: 3
names: ['gun', 'knife', 'bat']
